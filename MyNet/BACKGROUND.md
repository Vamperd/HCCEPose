# MyNet 背景说明

## 1. 项目定位

`MyNet` 是一个面向 **DJI Action 4 单类目标** 的 8 角点检测子项目。

本项目的目标不是恢复 6D 位姿，而是建立一条完整的 8 角点检测路线：

1. 获取 DJI Action 4 的三维模型
2. 使用 HCCEPose / BlenderProc 渲染合成训练数据
3. 从渲染数据中构建 8 个包围盒角点标签
4. 训练一个输入为单目 RGB 图像、输出为 8 个角点的网络
5. 在真实 DJI Action 4 图像上验证角点预测效果

`MyNet` 的职责是承接这条路线中“8 角点监督”和“8 角点网络”这两部分。

## 2. 设计动机

### 2.1 当前任务是单类目标

当前研究对象是 DJI Action 4，一个固定类别的具体物体。

因此，项目更适合围绕单类目标建立一条稳定、可控、可验证的角点检测流程，而不是优先面向大规模多类别泛化问题。

### 2.2 当前仓库已经具备渲染数据生成能力

HCCEPose 已经提供了 Blender 渲染能力，能够输出：

- RGB 图像
- 深度图
- mask
- 可见 mask
- 相机参数
- 物体位姿真值

这意味着合成数据生成已经具备基础，当前更需要补齐的是：

- 8 个角点的监督格式
- 8 个角点的检测网络

### 2.3 当前目标需要显式角点表达

8 个包围盒角点具有清晰的几何含义，适合作为直接监督目标。

这种表示方式有以下特点：

- 标签定义明确
- 可视化检查方便
- 便于统一训练标签格式
- 适合做 heatmap 形式监督

因此，本项目选择以 **8 个包围盒角点** 作为网络输出目标。

### 2.4 当前应用场景是单目 RGB

目标系统的外部输入形式是单目 RGB 图像，而不是 RGB-D。

因此，本项目主线围绕单目 RGB 构建，深度图只作为渲染结果中的附加信息，不作为第一版主输入。

## 3. 设计路径

### 3.1 第一阶段：三维模型准备

输入：

- DJI Action 4 的三维模型

目标：

- 将目标物体整理为 HCCEPose 可用的 BOP 模型格式

产物：

- `obj_000001.ply`
- `models_info.json`

### 3.2 第二阶段：合成数据渲染

输入：

- BOP 格式物体模型
- BlenderProc / HCCEPose 渲染脚本

目标：

- 生成用于角点检测训练的 `train_pbr` 数据

产物：

- RGB 图像
- 深度图
- mask
- 可见 mask
- 相机参数
- 物体位姿真值

### 3.3 第三阶段：角点标签构建

输入：

- `train_pbr`
- `models_info.json`

目标：

- 从现有 BOP 数据中生成 8 个包围盒角点监督

产物：

- 8 个三维角点
- 8 个二维投影角点
- 8 通道 heatmap 或等价监督格式

### 3.4 第四阶段：8 角点网络训练

输入：

- 单目 RGB 图像
- 8 角点标签

目标：

- 训练一个能够预测 8 个角点位置的网络

产物：

- 8 角点预测模型

### 3.5 第五阶段：真实测试

输入：

- 真实 DJI Action 4 图像

目标：

- 验证网络能否稳定预测 8 个角点

产物：

- 角点预测结果
- 角点可视化结果

## 4. 项目中各模块的职责划分

### 4.1 HCCEPose 的职责

当前仓库中的 HCCEPose 负责：

- 管理 BOP 格式数据
- 渲染生成 `train_pbr`
- 提供物体模型与真值信息

HCCEPose 在本项目中是 **数据生成基础模块**。

### 4.2 MyNet 的职责

`MyNet` 负责：

- 读取 HCCEPose 生成的数据
- 构建 8 角点监督格式
- 实现 8 角点预测网络
- 输出 8 个角点结果

`MyNet` 在本项目中是 **8 角点检测模块**。

## 5. 网络方案定位

### 5.1 主线方案

本项目的主线方案是：

```text
detector + ROI crop + 8-corner heatmap network
```

即：

1. 先获得目标实例的 2D 区域
2. 对单个实例区域进行裁剪
3. 在裁剪区域内预测 8 个角点 heatmap
4. 解码得到 8 个二维角点

### 5.2 Backbone 选择

主线采用：

- `ResNet` 系列 backbone

备选方案保留：

- `ViT`

当前明确采用：

- **ResNet 主线**
- **ViT 备选**

## 6. 训练与测试策略

### 6.1 训练策略

当前训练策略为：

- 仅使用合成渲染数据训练

即：

- 使用 `train_pbr`
- 不在第一阶段引入真实数据微调

### 6.2 测试策略

当前测试策略为：

- 在真实 DJI Action 4 图像上测试

重点验证：

- 8 个角点是否能稳定定位
- 不同视角下角点预测是否一致

## 7. 需要实现的目标

### 7.1 数据层目标

需要完成：

- 从 `train_pbr` 中提取训练所需信息
- 构建实例级样本
- 构建 8 角点监督标签

### 7.2 模型层目标

需要完成：

- 搭建 8 角点预测网络
- 定义网络输入输出
- 定义训练监督形式

### 7.3 几何层目标

需要完成：

- 定义固定的 8 个三维角点顺序
- 将三维角点投影为二维角点
- 保持训练和推理阶段的角点顺序一致

### 7.4 实验层目标

需要完成：

- 在合成数据上完成训练
- 在真实数据上完成测试
- 检查角点预测结果是否稳定

## 8. 当前阶段的明确目标

当前阶段的目标不是一次性完成整个系统，而是按顺序推进：

1. 明确数据基础
2. 明确背景与路线
3. 明确 8 角点标签构建方式
4. 明确网络主线结构
5. 再进入代码实现

因此，`MyNet` 当前文档的作用是：

- 把背景讲清楚
- 把设计动机讲清楚
- 把项目目标讲清楚
- 把后续实现边界讲清楚

## 9. 项目目标总结

`MyNet` 的最终目标是：

> 基于 HCCEPose 渲染得到的 DJI Action 4 合成数据，构建一个以单目 RGB 图像为输入、以 8 个包围盒角点为输出的单类角点检测网络。

这就是本项目的整体背景、设计动机、设计路径和实现目标。
